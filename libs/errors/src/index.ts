import { StatusCodes } from './config'

export const NOT_FOUND_CODE = StatusCodes.NOT_FOUND;

export const FORBIDDEN_CODE = StatusCodes.FORBIDDEN;

export const UNAUTHORIZED_CODE = StatusCodes.UNAUTHORIZED;
